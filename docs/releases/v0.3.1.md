# xctidy v0.3.1

## Fix

Two SwiftLint `line_length` violations in `-fv` code that only failed
under `--strict` (CI), after v0.3.0's `-fv` code landed. No behavior
change.

(Never had a full GitHub Release drafted for this tag -- only the
annotated tag message. `v0.3.2` picks up the rest of this same CI-red
saga with a second, unrelated `--strict` violation.)
